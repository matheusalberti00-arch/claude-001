<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net10.0-android</TargetFramework>
		<OutputType>Exe</OutputType>
		<RootNamespace>CorpoSync</RootNamespace>
		<UseMaui>true</UseMaui>
		<SingleProject>true</SingleProject>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<SkipValidateMauiImplicitPackageReferences>true</SkipValidateMauiImplicitPackageReferences>

		<!-- Nome e identidade do app -->
		<ApplicationTitle>CorpoSync</ApplicationTitle>
		<ApplicationId>com.matheus.corposync</ApplicationId>
		<ApplicationDisplayVersion>0.5.1</ApplicationDisplayVersion>
		<ApplicationVersion>6</ApplicationVersion>

		<SupportedOSPlatformVersion>21</SupportedOSPlatformVersion>
	</PropertyGroup>

	<ItemGroup>
		<!-- Ícone do app e tela de abertura (splash) -->
		<MauiIcon Include="Resources\AppIcon\appicon.svg" ForegroundFile="Resources\AppIcon\appiconfg.svg" Color="#2E7D6F" />
		<MauiSplashScreen Include="Resources\Splash\splash.svg" Color="#2E7D6F" BaseSize="128,128" />
	</ItemGroup>

	<ItemGroup>
		<!-- Bluetooth Low Energy (mesma biblioteca do projeto de referência) -->
		<PackageReference Include="Plugin.BLE" Version="3.2.1" />
	</ItemGroup>

</Project>
